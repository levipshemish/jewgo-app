# Frontend-specific Python dependencies
# These are only needed for frontend build processes, not backend runtime

# Web scraping and browser automation (for frontend testing/SSR)
playwright==1.54.0

# Image processing (if needed for frontend)
# pillow==10.0.0

# Note: This file is separate from backend/requirements.txt
# Backend should only install what it needs to run the API
