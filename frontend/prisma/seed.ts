/**
 * Seed script for admin roles using PostgreSQL auth.
 * Usage:
 *   - Set ADMIN_SEED_SUPERADMIN_EMAIL to an existing user's email.
 *   - Run: npx tsx frontend/prisma/seed.ts
*/
// PostgreSQL auth - using direct database connection instead of Supabase

console.log('Seed script not implemented for PostgreSQL auth');
console.log('Please use the backend API to manage admin roles');

process.exit(0);