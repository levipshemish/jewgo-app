# Core Flask dependencies
Flask==2.3.3
Flask-CORS==4.0.0
Flask-SocketIO==5.5.1
python-socketio==5.13.0
python-engineio==4.12.2
Flask-Caching==2.1.0
Flask-Session==0.8.0

# Monitoring and performance
prometheus-client==0.20.0
sentry-sdk==2.35.0

# Database dependencies - PostgreSQL
psycopg2-binary==2.9.10
SQLAlchemy==2.0.43

# Redis dependencies
redis==5.0.1
flask-socketio==5.3.6

# Environment and configuration
python-dotenv==1.0.0

# Utilities
requests==2.31.0
structlog==24.1.0
python-dateutil==2.8.2

# Security and authentication
PyJWT==2.8.0
cryptography==44.0.0
bcrypt==4.1.3
Authlib>=1.3.0
Flask-Limiter==3.5.0

# Scheduling and background tasks
APScheduler==3.10.4
tzlocal>=2,<3  # Companion pin to avoid APScheduler 3.10.x pulling incompatible tzlocal
pytz==2025.1  # Updated for current DST rules
# Production note: Use external job store (SQLAlchemy/Redis) and configure jobs with max_instances=1 and coalesce=True for multi-worker deployments

# Production server
gunicorn==21.2.0
gevent==23.9.1
greenlet>=3.0.0,<4.0.0  # Allow flexible greenlet version for compatibility

# Script dependencies (for CI validation)
click==8.1.7
PyYAML==6.0.1

# System monitoring
psutil==5.9.8

# Browser automation (for auth API) - using latest version for better compatibility
playwright>=1.40.0
